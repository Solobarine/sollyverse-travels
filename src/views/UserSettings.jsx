import Settings from '../components/Settings';
import Menu from '../components/Menu';

const UserSettings = () => {
  return (
    <section className="userSetting page shrinkMenu">
      <Menu />
      <Settings />
    </section>
  )
}

export default UserSettings;
